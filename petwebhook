-- Сначала устанавливаем FPS отдельно
setfpscap(5)

-- Затем задаем конфигурацию
getgenv().Config = {
    AutoBlowBubble     = true,
    AutoSellBubble     = true,
    AutoBuyGum         = true,
    AutoHatchEggs      = true,
    AutoUnlockIslands  = true, -- dont change this if u dont know how it works
    AutoEquipBestPets  = true,
    AutoClaimRewards   = true,
    AutoUpgradeMasteryAndClaim = true,
    AutoClaimSeason = true,
    AutoCollectDrop = true, -- dont change this if u dont know how it works
    AutoDeletePets = true, -- dont change this if u dont know how it works
    SellInterval       = 4,   -- dont change this if u dont know how it works
    ESpamDuration      = 3,   -- dont change this if u dont know how it works
    IslandUnlockAttempts = 2, -- dont change this if u dont know how it works
    PerformanceMode    = false,
    SetDefaultEggToHatch = false,
    TargetEgg = "Rainbow Egg", -- Egg to open, default u can SetDefaultHatch and it will basic hatch eggs...
    FarmCoinsAmount = 50000000 -- Money farm before start hatch egg, if u already have 1b or more , then it will open if u specify 1b, i think u understand
}

-- Загружаем основной скрипт
loadstring(game:HttpGet("https://raw.githubusercontent.com/fornamess/BubbleGumSimulator/refs/heads/main/Egg.lua"))()
